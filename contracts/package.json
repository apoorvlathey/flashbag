{
  "scripts": {
    "gd": "source .env && forge script ./script/deploy.s.sol --broadcast --rpc-url $GOERLI_RPC_URL --sender $SENDER --private-key $PRIVATE_KEY",
    "md": "source .env && forge script ./script/deploy.s.sol --broadcast --rpc-url $MUMBAI_RPC_URL --sender $SENDER --private-key $PRIVATE_KEY"
  },
  "dependencies": {
    "@connext/nxtp-contracts": "0.2.1-beta.13",
    "@openzeppelin/contracts-upgradeable": "^4.8.0"
  }
}
